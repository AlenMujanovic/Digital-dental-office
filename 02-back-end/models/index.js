const { Appointment } = require('./appointment');
const { User } = require('./user');
const { Prescription } = require('./prescription');

module.exports = {
  User,
  Appointment,
  Prescription,
};
